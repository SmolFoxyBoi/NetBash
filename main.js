let con_conf = {
	width: 640,
	height: 320,
	font: "Consolas",
	font_size: 12,
	prompt: "$ "
}
let con = netbash.new("here", con_conf)
/*con.in_program = true
con.write("enter a value: ")
let out = con.readln()
con.writeln(out)
con.in_program = false*/